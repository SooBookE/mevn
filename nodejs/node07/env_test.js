// const dotenv = require('dotenv')
// dotenv.config()
// dotenv 설치하고 .env에 환경변수 설정한 후 위에 두 줄을 삽입하면 쉽게 설정가능.

const out = process.env.key
console.log(out)
// const out = process.env.akey
// console.log(out)
